import Sidebar from "@/components/ui/sidebar";

const Schedule = () => {
    return (
        <div className="flex flex-row">
            <Sidebar />
        </div>
    );
};

export default Schedule;
